[project]
name = "sql-test-generator"
version = "0.1.0"
description = "Generates plain-English data quality test cases from SQL queries using AWS Bedrock"
readme = "README.md"
authors = [
    { name = "sagar1208", email = "60442468+sagar1208@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.0",
]

[tool.uv]
package = false
